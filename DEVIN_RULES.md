# Quy tắc bắt buộc cho Devin Sessions

1. Push code lên branch `viet` — KHÔNG ĐƯỢC push main
2. Không tạo PR trừ khi user yêu cầu
3. Không nói "CI pass" nếu chưa có CI chạy thật
4. Telegram bot test = chạy run.bat trên local, KHÔNG dùng Vercel
5. Trước khi báo hoàn thành: chạy `python -m py_compile` cho mọi .py file đã sửa
6. Nếu có lỗi import hoặc syntax: fix trước khi commit
7. Ghi rõ danh sách file đã thay đổi trong commit message
