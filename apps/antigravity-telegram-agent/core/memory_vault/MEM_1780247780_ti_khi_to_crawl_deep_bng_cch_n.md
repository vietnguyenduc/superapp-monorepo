# Task Objective
Tôi khởi tạo crawl deep bằng cách nào, có command nào dễ nhớ không?

# Strategy Used
Direct self-healing command execution.

# Code Snippets (Skills)
🌐 _[DeepSeek]_

🌐 _[DeepSeek]_

## ✅ Đã kiểm tra code — Đây là cách dùng Deep Crawl

### 🎯 Lệnh dễ nhớ nhất:

```
/crawl --deep <url>
```

Ví dụ:
```
/crawl --deep https://vnexpress.net
```

### 📋 Tất cả các lệnh crawl hiện có:

| Lệnh | Chế độ | Token | Chất lượng |
|---|---|---|---|
| `/crawl <url>` | Python thuần (nhanh) | **0** | Cơ bản (title, meta, links, text) |
| `/crawl --deep <url>` | Hybrid (AI 1 lần) | **~2000** | Cao (phân loại, entity, summary, insights) |
| `/crawl --deep --model 

# Lessons Learned
Execution completed with success=True and complexity=medium.