# Task Objective
Bạn fix đi

# Strategy Used
Direct self-healing command execution.

# Code Snippets (Skills)
🌐 _[DeepSeek]_

## ✅ Đã phân tích — Đây là nguyên nhân và cách fix

### 🔍 Chẩn đoán

**Nguyên nhân gốc rễ:** Package `@superapp/iam` **KHÔNG có thư mục `dist/`** — chỉ có `src/`.

Cụ thể:
- `packages/iam/package.json` trỏ `"main": "src/index.ts"` — nghĩa là nó **chỉ chạy được khi được transpile on-the-fly** (VD: qua Vite alias hoặc tsconfig paths)
- `packages/iam/tsconfig.json` có `"outDir": "./dist"` nhưng **chưa bao giờ chạy `tsc`** để build ra dist
- `ProtectedRoute.tsx` gọi `useAuthContext()

# Lessons Learned
Execution completed with success=True and complexity=medium.