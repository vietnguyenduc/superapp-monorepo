# Task Objective
The primary objective was to identify and fix various issues across several test files and core code components to ensure correct functionality and pass all tests. This included updating role-based access control (RBAC) configurations, adding missing error codes, correcting data formatting expectations, and resolving inconsistencies in data parsing and validation logic. The ultimate goal was to achieve zero failures after all fixes were applied and accounting verification was run.

# Strategy Used
The strategy involved an iterative approach, addressing specific files and their identified issues one by one. The process started with fixing known discrepancies in `rbac.test.ts`, `errorHandling.ts`, and `formatting.test.ts`. Subsequently, the focus shifted to `importUtils.test.ts` to correct field names and test data. For files with suite errors (`transactionTypeNames.test.tsx`, `dashboardMetrics.test.ts`, `backupRecovery.test.ts`), the strategy was to investigate the root cause of the errors (e.g., missing imports, non-existent functions, export issues) and then apply the necessary fixes. The task was paused before all issues were resolved, indicating a phased execution.

# Code Snippets (Skills)
- **`rbac.test.ts`**:
    - Changed `usePermissions("admin")` to `usePermissions("admin_master")`.
    - Changed `usePermissions("branch_manager")` to `usePermissions("admin_company")`.
    - Updated `dashboardView?.roles` expectation from `"admin"` to `"admin_master"`.
    - Updated `customersDelete?.roles` expectation from `["admin"]` to `["admin_master", "admin_company"]`.
    - Updated `ROLE_HIERARCHY.admin` to `ROLE_HIERARCHY.admin_master`.
    - Updated `ROLE_HIERARCHY.branch_manager` to `ROLE_HIERARCHY.admin_company`.
- **`errorHandling.ts`**:
    - Added `DATABASE_CONNECTION_FAILED: "database_connection_failed"` to `ERROR_CODES`.
- **`formatting.test.ts`**:
    - Corrected `formatTransactionType("payment")` expectation to `"Phát sinh giảm"`.
    - Corrected `formatTableCell("payment", "type")` expectation to `"Phát sinh giảm"`.
- **`importUtils.test.ts`**:
    - Patched `toEqual` in parse tab-separated data test (lines 18-33) by changing `customer_name` to `customer_code`.
    - Identified remaining ~17 occurrences of `customer_name` that need to be changed to `customer_code` in various test data objects, assertions, and error messages (e.g., `result[0].customer_name`, `validData`, `column: "customer_name"`, `"Customer name is required"`).
- **`transactionTypeNames.test.tsx`**:
    - Identified suite error due to non-existent `TransactionTypeContext` import.
- **`dashboardMetrics.test.ts`**:
    - Identified suite error due to non-existent `databaseService.dashboard.getDashboardMetrics`.
- **`backupRecovery.test.ts`**:
    - Identified suite error requiring verification of exports from `backupRecovery.ts`.

# Lessons Learned
- **Succeeded:**
    - Successfully applied targeted patches to `rbac.test.ts`, `errorHandling.ts`, and `formatting.test.ts`, resolving specific issues related to role definitions, error code availability, and data display logic.
    - Demonstrated the ability to identify and correct specific data field inconsistencies in test cases, as shown by the partial fix in `importUtils.test.ts`.
- **Failed/Uncompleted:**
    - The task was not fully completed within the given time/run limits, leaving several critical fixes pending.
    - A significant number of `customer_name` references in `importUtils.test.ts` still need to be updated to `customer_code`, indicating a widespread inconsistency that requires thorough review.
    - Several test suites (`transactionTypeNames.test.tsx`, `dashboardMetrics.test.ts`, `backupRecovery.test.ts`) failed due to fundamental issues like missing imports or non-existent functions, highlighting the need for deeper investigation into the codebase's structure and dependencies.
    - The final verification step ("Chạy verify accounting") could not be performed, meaning the overall objective of achieving zero failures was not met.
- **Errors Healed/Identified:**
    - Identified and corrected outdated role names and permissions in RBAC tests.
    - Addressed a missing error code for a common database failure scenario.
    - Rectified incorrect expected outputs for transaction type formatting, ensuring UI consistency.
    - Pinpointed a pervasive `customer_name` vs `customer_code` inconsistency in import utility tests, which impacts data parsing, validation, and error messaging.
    - Diagnosed suite-level errors in other test files, pointing to issues with module imports, function availability, or export configurations.