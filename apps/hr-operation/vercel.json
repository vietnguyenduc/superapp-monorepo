{
    "buildCommand": "cd ../.. && npx turbo run build --filter=hr-operation...",
    "outputDirectory": "dist",
    "installCommand": "cd ../.. && npm install",
    "framework": "vite",
    "rewrites": [
        {
            "source": "/(.*)",
            "destination": "/index.html"
        }
    ]
}